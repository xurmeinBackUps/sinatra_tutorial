<h1>Articles with <%= @tag.name %> tag</h1>

<ul>
    <% @tag.articles.each do |article| %>
        <li><%= link_to article.title, article_path(article) %></li>
        <p><%= link_to 'DELETE', tag_path(@tag), method: :delete, data: {confirm: 'Yeah? Delete tag?'}%></p>
    <% end %>
</ul>

